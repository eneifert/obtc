<li class="<%= filter %> col-xs-6 col-sm-4 col-md-3 col-lg-3">
  <div class="lightCon"> 
    <span class="hoverBox"> 
      <span class="smallIcon"> 
        <a rel="lightbox-demo" href="<%= img_url_for_env("staff/#{big}") %>" title="<%= title %>" class="zoom lb lb_warsaw1"><i class="fa fa-search fa-2x"></i></a> 
        <% if link.length > 0 %>
          <a href="<%= link %>" title="Project Link" target="_blank" class="linKed"><i class="fa fa-link fa-2x"></i></i></a> 
        <% end %>
      </span> 
    </span> 
    <img src="<%= img_url_for_env("staff/#{img}") %>" alt=""  > 
  </div>
</li>